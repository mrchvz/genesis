<%= render 'website/partials/header_dashboard' %>
<div class='container'>
  <div class='row dashboard'>
    <div class='col-3 sidebar'>
      <%= render 'website/partials/dashboard_sidebar' %>
    </div>
    <div class='col-9 content'>
      <h2 class='subtitle'>
        Delaware Incorporation
      </h2>
      <h4 class='status'>
        Form is accepted
      </h4>
      <h5 class='disclaimer'>
        Your application was accepted at 1/1/1 1:11pm by Sergio Sufy,
        the estimated time for completion for this process in particular is around 2 weeks,
        for your convenience you can visualize the list of steps below, if you want to receive notifications by email,
        please click the subscribe button in the top right corner.
      </h5>
      <h4 class='timeline'>
        TIMELINE
      </h4>
      <div class="progress">
        <div class="progress-bar" role="progressbar" style="width: 20%;" aria-valuenow="20" aria-valuemin="0" aria-valuemax="100">ONE</div>
      </div>
      <%= render 'website/partials/instance_details' %>
      <%= render 'website/partials/instance_payment_methods' %>
      <%= render 'website/partials/instance_payments' %>
      <%= render 'website/partials/instance_invoices' %>
      <%= render 'website/partials/instance_history' %>
      <%= render 'website/partials/instance_comments' %>
    </div>
  </div>
</div>
<%= render 'website/partials/footer_dashboard' %>
